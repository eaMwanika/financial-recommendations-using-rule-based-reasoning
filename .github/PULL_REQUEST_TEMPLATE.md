## What does this PR change?

<!-- One or two sentences describing what was added, fixed, or changed. -->

## Which part of the project does this touch?

- [ ] Knowledge base (`knowledge.json`, `knowledge_base.py`)
- [ ] Inference engine (`InferenceEngine.py`)
- [ ] Financial calculations (`financialCalculator.py`)
- [ ] Web UI (`app.py`, `templates/`, `static/`)
- [ ] CLI (`cli.py`)
- [ ] Knowledge acquisition (`add_rule.py`)
- [ ] Tests (`tests/`)
- [ ] Documentation (`README.md`, etc.)
- [ ] Other:

## How was this tested?

<!-- e.g. "Ran `python -m unittest discover -s tests -v`, all N tests pass."
     or "Manually ran cli.py / app.py with sample inputs X, Y, Z." -->

## Checklist

- [ ] My branch is named `feature/<my-name>-<short-description>`
- [ ] I made at least 3 commits with descriptive messages (e.g. "Added disease rules", not "update")
- [ ] Existing tests still pass (`python -m unittest discover -s tests -v`)
- [ ] I did not modify files owned by another feature without discussing it first
- [ ] This PR is ready for review (not a draft / work in progress)
