# Screenshots

This folder should contain, before final submission:
1. `terminal_cli_run.png` - screenshot of the CLI menu and a sample advice run
2. `test_results.png` - screenshot of `python -m unittest discover -s tests` passing
3. `flask_ui.png` - screenshot of the Flask web form and results page in a browser
4. `github_repo.png` - screenshot of the GitHub repository page showing commit history
5. `pull_request.png` - screenshot of the merged Pull Request

Text captures of the CLI run and test output are included here
(`cli_sample_run.txt`, `test_run_output.txt`) as a placeholder/reference
until the actual PNG screenshots are added.
